{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Zombies++",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/prog",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "preLaunchTask": "Compilar Zombies++",
            "windows": { "program": "${workspaceFolder}/prog.exe" }
        }
    ]
}
